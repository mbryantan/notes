{
  "routes": [
    {
      "path": "/api/events",
      "methods": ["GET"],
      "handler": {
        "module": "$import(@/modules/events-handler)",
        "export": "handleEvents"
      },
      "policies": [
        {
          "policyName": "api-key-auth"
        }
      ]
    }
  ]
}
